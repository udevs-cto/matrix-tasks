#include <iostream>
#include <cmath>
#include <cassert>

/*
void runTests() {
    // Test Circle
    Circle circle("Red", std::make_pair(0, 0), 5);
    double circleArea = circle.calculateArea();
    double circlePerimeter = circle.calculatePerimeter();
    std::cout << "Circle Area: " << circleArea << " Perimeter: " << circlePerimeter << std::endl;
    assert(std::abs(circleArea - 78.5375) < 0.01); // Test area with a tolerance of 0.01
    assert(std::abs(circlePerimeter - 31.415) < 0.01); // Test perimeter with a tolerance of 0.01

    // Test Rectangle
    Rectangle rectangle("Blue", std::make_pair(0, 0), 6, 4);
    double rectangleArea = rectangle.calculateArea();
    double rectanglePerimeter = rectangle.calculatePerimeter();
    std::cout << "Rectangle Area: " << rectangleArea << " Perimeter: " << rectanglePerimeter << std::endl;
    assert(rectangleArea == 24);
    assert(rectanglePerimeter == 20);

    // Test Triangle
    Triangle triangle("Green", std::make_pair(0, 0), 8, 4);
    double triangleArea = triangle.calculateArea();
    double trianglePerimeter = triangle.calculatePerimeter();
    std::cout << "Triangle Area: " << triangleArea << " Perimeter: " << trianglePerimeter << std::endl;
    assert(triangleArea == 16);
    assert(trianglePerimeter == 24);
}

int main() {
    runTests();
    return 0;
}
*/